# In-process mqtt-lib broker (used by `cargo test`).
# Addresses are empty because the harness binds to a random loopback port.
# Primarily used for parser tests and fast iteration during development.

name = "mqtt5-inprocess"
address = ""
tls_address = ""
ws_address = ""

[capabilities]
max_qos = 2
retain_available = true
wildcard_subscription_available = true
subscription_identifier_available = true
shared_subscription_available = true
topic_alias_maximum = 65535
server_receive_maximum = 65535
assigned_client_id_supported = true
injected_user_properties = ["x-mqtt-sender", "x-mqtt-client-id"]
strict_client_id_charset = true
dollar_sys_publish = true

[capabilities.transports]
tcp = true
tls = false
websocket = false
quic = false
